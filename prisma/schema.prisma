datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model Contador {
  id    Int    @id @default(autoincrement())
  contagem Int
}